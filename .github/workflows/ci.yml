name: CI & Quality Gate

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_call:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    name: Verification & Quality Gate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build test image
        run: docker buildx build --load --target test --tag portfolio-test --cache-from type=gha --cache-to type=gha,mode=max .

      - name: Run PHPUnit Test Suite
        run: docker run --rm --env-file .env.example -e APP_ENV=test portfolio-test vendor/bin/phpunit --fail-on-phpunit-notice

      - name: Run JavaScript Test Suites
        run: docker run --rm -v "$PWD:/app" -w /app node:24-alpine sh -lc 'for test in tests/js/*.test.js; do node "$test" || exit 1; done'

      - name: Run PSR-12 Code Style
        run: docker run --rm portfolio-test vendor/bin/phpcs

      - name: Run PHPStan Static Analysis
        run: docker run --rm portfolio-test vendor/bin/phpstan analyse --no-progress --memory-limit=512M

      - name: Lint Twig Templates
        run: docker run --rm portfolio-test php bin/console lint:twig templates

      - name: Lint YAML Configuration & Translations
        run: docker run --rm portfolio-test php bin/console lint:yaml translations config

      - name: Validate Composer Configuration
        run: docker run --rm portfolio-test composer validate --strict --no-check-publish

      - name: Audit Composer Dependencies
        run: docker run --rm portfolio-test composer audit --locked --no-interaction
